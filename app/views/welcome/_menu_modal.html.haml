/ Menu modal

#menuModal.modal.hide.fade
  / The header is mostly useful for the "x" that closes it.
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} 
    %h3 Your Order


  .modal-body
    / TODO: I don't know if this is a great place to begin the form.
    = form_for :order, url: orders_path do |f|
      - @form = f
      .row-fluid.center
        / Name and deliver location
        = render "menu_info"
        = render "menu_receipt"
          
      = render "menu_content"
